- name: cv_db
  kind: postgres
  configuration:
    connection_info:
      database_url: postgres://postgres:postgres@cv-db:5432/postgres
      isolation_level: read-committed
      use_prepared_statements: false
  tables: "!include cv_db/tables/tables.yaml"
